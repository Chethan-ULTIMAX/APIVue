/** Codeforces normalizer. */
import type { PublicActivity, PublicBreakdown, PublicDataResult, PublicMetric, PublicProfile } from '../types';
import type { RawCodeforcesProfile, RawCodeforcesSubmission } from '../fetchers/codeforces';
function toPublicProfile(raw: RawCodeforcesProfile): PublicProfile { const { user } = raw; return { platform: 'codeforces', username: user.handle, displayName: [user.firstName, user.lastName].filter(Boolean).join(' ').trim() || null, avatarUrl: user.avatar ?? user.titlePhoto ?? null, profileUrl: `https://codeforces.com/profile/${encodeURIComponent(user.handle)}`, bio: user.organization?.trim() || null, location: [user.city, user.country].filter(Boolean).join(', ') || null, joinedAt: new Date(user.registrationTimeSeconds * 1000).toISOString() }; }
function submissionTitle(sub: RawCodeforcesSubmission) { return [sub.problem.rating ? `${sub.problem.rating} rated` : '', sub.verdict ?? '', sub.programmingLanguage ?? ''].filter(Boolean).join(' · '); }
function toPublicActivity(sub: RawCodeforcesSubmission, profileUrl: string): PublicActivity { return { id: String(sub.id), title: sub.problem.name, description: submissionTitle(sub) || undefined, timestamp: new Date(sub.creationTimeSeconds * 1000).toISOString(), url: sub.contestId ? `https://codeforces.com/contest/${sub.contestId}/problem/${sub.problem.index}` : profileUrl, type: 'submission' }; }
function buildTagBreakdown(submissions: RawCodeforcesSubmission[]): PublicBreakdown { const counts = new Map<string, number>(); for (const sub of submissions) if (sub.verdict === 'OK') for (const tag of sub.problem.tags ?? []) counts.set(tag, (counts.get(tag) ?? 0) + 1); const total = [...counts.values()].reduce((a, b) => a + b, 0); return { label: 'Tags solved', items: [...counts.entries()].map(([label, value]) => ({ label, value, percentage: total ? Math.round(value / total * 1000) / 10 : 0 })).sort((a, b) => b.value - a.value).slice(0, 10) }; }
export function normalizeCodeforcesData(raw: RawCodeforcesProfile): PublicDataResult { const { user, ratings, submissions } = raw; const accepted = submissions.filter((s) => s.verdict === 'OK'); const profileUrl = `https://codeforces.com/profile/${encodeURIComponent(user.handle)}`; return { platform: 'codeforces', profile: toPublicProfile(raw), metrics: [{ label: 'Current rating', value: user.rating ?? 'Unrated' }, { label: 'Max rating', value: user.maxRating ?? '—' }, { label: 'Current rank', value: user.rank ?? 'Unrated' }, { label: 'Max rank', value: user.maxRank ?? '—' }, { label: 'Accepted recently', value: accepted.length }, { label: 'Rated contests', value: ratings.length }, { label: 'Contribution', value: user.contribution }, { label: 'Friends', value: user.friendOfCount }], activity: submissions.map((s) => toPublicActivity(s, profileUrl)).sort((a, b) => b.timestamp.localeCompare(a.timestamp)), breakdowns: [buildTagBreakdown(submissions)], ratingHistory: ratings.map((r) => ({ date: new Date(r.ratingUpdateTimeSeconds * 1000).toISOString(), value: r.newRating })), fetchedAt: new Date().toISOString(), source: 'public-api' }; }
