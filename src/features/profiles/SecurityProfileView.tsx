import { ExternalLink, Layers3, RefreshCw, ShieldCheck, Trophy } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { Badge } from '@/components/ui/badge';
import type { ProfileSnapshot, TrackedProfile } from '@/lib/integrations/types';
import { ProfileAvatar } from '@/components/apivue/ProfileBits';
import { ActivityHeatmap, HistoryChart, type ActivityPoint } from '@/components/apivue/ProfileAnalyticsKit';
import './profile-analytics.css';
function value(profile:TrackedProfile,key:string){return profile.data?.metrics?.find((m)=>m.key===key)?.value}function fmt(v:string|number|undefined){return typeof v==='number'?v.toLocaleString():v??'—'}
export function SecurityProfileView({profile,snapshots,refreshing,onRefresh}:{profile:TrackedProfile;snapshots:ProfileSnapshot[];refreshing:boolean;onRefresh:()=>void}){const isThm=profile.platform==='tryhackme',metrics=profile.data?.metrics??[],activity=(profile.data?.activity??[]) as ActivityPoint[],history=snapshots.slice().sort((a,b)=>a.captured_at.localeCompare(b.captured_at)).flatMap(s=>Object.entries(s.metrics??{}).slice(0,1).map(([key,v])=>({date:s.captured_at.slice(0,10),value:Number(v),label:key.replaceAll('_',' ')}))),avatarProfile={...profile,avatarUrl:profile.avatarUrl??profile.data?.avatarUrl??null,displayName:profile.displayName??profile.data?.displayName??profile.handle};return <div className="mx-auto max-w-7xl space-y-5 p-4 sm:p-6"><div className="rounded-3xl border bg-gradient-to-br from-emerald-500/[0.10] via-background to-background p-5 shadow-sm sm:p-7"><div className="flex flex-col gap-5 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-4"><div className="relative flex h-[88px] w-[88px] shrink-0 items-center justify-center rounded-full"><ProfileAvatar profile={avatarProfile} size="xl"/></div><div><div className="flex flex-wrap items-center gap-2"><Badge variant="outline" className="border-emerald-500/30">Public profile</Badge><Badge variant="outline">Cybersecurity</Badge></div><h1 className="mt-2 text-2xl font-bold tracking-tight">{profile.displayName||profile.handle}</h1><p className="text-sm text-muted-foreground">@{profile.handle}</p><p className="mt-1 text-xs text-muted-foreground">{profile.data?.dataNotice??'Public platform data. Ownership is not verified by APIVue.'}</p></div></div><div className="flex gap-2"><Button variant="outline" onClick={onRefresh} disabled={refreshing}><RefreshCw className={`mr-2 h-4 w-4 ${refreshing?'animate-spin':''}`}/>Refresh</Button>{(profile.profileUrl??profile.profile_url)&&<a href={profile.profileUrl??profile.profile_url} target="_blank" rel="noreferrer noopener"><Button><ExternalLink className="mr-2 h-4 w-4"/>Open profile</Button></a>}</div></div></div>{isThm&&metrics.length?<div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{metrics.map((m)=><div key={m.key} className="rounded-2xl border bg-card p-5"><p className="text-xs text-muted-foreground">{m.label}</p><p className="mt-2 text-2xl font-bold">{fmt(m.value)}</p></div>)}</div>:<div className="rounded-2xl border border-dashed p-7"><div className="flex items-start gap-3"><Layers3 className="mt-0.5 h-5 w-5 text-muted-foreground"/><div><h2 className="font-semibold">Platform-published data</h2><p className="mt-1 text-sm text-muted-foreground">APIVue has a public profile link for this source but does not fabricate unavailable metrics. Use the platform profile for anything the provider does not publish through its supported endpoint.</p></div></div></div>}{isThm&&activity.length>0&&<ActivityHeatmap activity={activity} title="Security activity"/>}{isThm&&history.length>1&&<section className="rounded-2xl border bg-card p-5"><div className="mb-4 flex items-center gap-2"><ShieldCheck className="h-4 w-4 text-emerald-600"/><h2 className="font-semibold">Metric history</h2></div><HistoryChart points={history} valueLabel="Metric"/></section>}{isThm&&<div className="grid gap-5 lg:grid-cols-[1.4fr_1fr]"><div className="rounded-2xl border bg-card p-5"><div className="flex items-center gap-2"><ShieldCheck className="h-4 w-4 text-emerald-600"/><h2 className="font-semibold">Published metrics</h2></div><div className="mt-4 grid gap-2 sm:grid-cols-2">{metrics.map((m)=><div key={m.key} className="rounded-xl border p-3"><p className="text-xs text-muted-foreground">{m.label}</p><p className="mt-1 text-lg font-bold">{fmt(m.value)}</p></div>)}</div></div><div className="rounded-2xl border bg-card p-5"><div className="flex items-center gap-2"><Trophy className="h-4 w-4 text-amber-600"/><h2 className="font-semibold">Highlights</h2></div><div className="mt-4 space-y-2">{(profile.data?.highlights??[]).map((h,i)=><div key={`${h.title}-${i}`} className="rounded-xl border p-3"><p className="text-sm font-medium">{h.title}</p>{h.subtitle&&<p className="mt-1 text-xs text-muted-foreground">{h.subtitle}</p>}</div>)}{!profile.data?.highlights?.length&&<p className="text-sm text-muted-foreground">No additional highlights published.</p>}</div></div></div>}</div>}
